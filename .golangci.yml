linters:
  enable-all: true
  disable:
    - exhaustivestruct
    - wrapcheck
    - gofumpt
    - gci
    - godox
    - deadcode
  #deprecated
    - scopelint
    - maligned
    - interfacer
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - funlen
        - goconst
    - path: _mock\.go
      linters:
        - dupl
        - gomnd
        - forcetypeassert
